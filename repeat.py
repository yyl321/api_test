import os
import operator
import csv

rootdir = 'F:\\方向二题目一数据包\\stage1_dataset\\train\\white_api_to_txt'
dic = os.listdir(rootdir)
for i in dic:
    print(i)
    rep_lst=[]
    i = os.path.join(rootdir,i)
    with open(i,'r') as f:
        data = f.readlines()
    x=len(data)
    tmp=len(data)
    for i in range(0, x-1):

        if :
            rep_lst.append(j)
        x -= 1
        if x==1:
            break
    #print(rep_lst)
    break
    break
